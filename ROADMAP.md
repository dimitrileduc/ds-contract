# Roadmap

The public roadmap lives in the documentation: **[docs/12-roadmap.md](docs/12-roadmap.md)**.

## Prochaines specs (séquence arrêtée le 2026-07-31 · **état repris le 2026-08-05, à la clôture de 015**)

> **Comment lire cette section.** Les points 1 et 2 ci-dessous sont l'état d'avant l'exécution : ils sont conservés parce qu'ils portent le raisonnement qui a dimensionné le chantier, mais **014 et 015 sont closes** et une de leurs prescriptions s'est révélée fausse à la mesure — voir « Ce que l'exécution a corrigé » juste après. La source vive de ce qui reste est le registre `specs/014-mesure-juste-triage/proofs/registre/causes.json § deferredWork` (lu par la porte de mesure, jamais de la prose) ; le tableau lisible est `specs/015-geometrie-gouvernee/RAPPORT-CLOTURE.md` §5.

Ordre imposé par les dépendances — chaque spec dimensionne la suivante, gates machine fail-closed entre elles (modèle `evaluateWaveEntry`) :

1. **014 — `mesure-juste-triage`** (**close**) : DW-006 (référence = node du cas, jamais le set), triage des 4 lignes UNTRIAGED, `select` mesuré (34/34). Zéro correction — l'instrument et le classement seulement. Sa sortie (compte des écarts par cause) est l'entrée dimensionnante de 015.
2. **015 — géométrie gouvernée** (**close le 2026-08-05** — 208 valeurs invisibles → 0, `contract-geometry` 6 → 0, 183/183 ; ⚠️ la prescription DW-001 ci-dessous est périmée, voir plus bas) (Figma en lecture) : la géométrie des 34 contrats portée en références de tokens. Décisions à trancher EN PREMIER dans le plan : **box-sizing — TRANCHÉ le 2026-08-04, c'est un oubli à corriger, pas une limite à nommer** (relevé complet : `DW-014-002` + reçu `react-box-sizing-absent`). Trois surfaces sur quatre sont déjà border-box — Figma via `strokeAlign: INSIDE`, `core/emit-html.ts` qui le déclare explicitement, le playground et la preview canvas. **Seul `core/emit-react.ts` ne le pose nulle part**, et aucun fichier de `src/` non plus : la bibliothèque livrée est la seule dehors. Portée mesurée : **9 contrats sur 34** ont une part liant à la fois une taille et du padding (accordion-row, carte, coordonnees, faq, footer, google-reviews, review-card, sav, textarea). Le commentaire de `emit-html` porte déjà le reçu du bug que la règle évite (piste du Switch 36×24 contre 32×20). **Et l'instrument ne peut pas le voir** : le harnais mesure l'émetteur HTML, qui a la bonne règle — la surface qui diverge n'est mesurée par rien sur cet axe. À corriger **avant** la conversion des littéraux : la règle décide quelle boîte le token décrit, convertir d'abord c'est convertir deux fois. Puis re-mesurer les 9 ; **DW-001 (largeur PiquerayLogo partagée footer/header) — TRANCHÉ le 2026-08-04, et ce n'était pas un arbitrage** : l'idiome existe et l'archive l'emploie (`avatar.contract.json`, demo-51) — une prop enum + un token dont le CHEMIN contient la prop (`"width": "{size.avatar.{size}}"`), soit une classe CSS par valeur et une variable liée par variante. Aucun des 51 contrats archivés n'utilise autre chose pour une taille. Travail : ajouter une prop `taille` à `ds.piqueray-logo` à côté de `couleur` (déjà gouvernée ainsi), minter `{size.logo.<valeur>}` — aucun token `logo` n'existe — et remplacer `literals: {width: 180px, height: 34px}` par la référence interpolée ; footer et header passent leur valeur comme ils passent déjà `couleur`. **DW-002 (cartes de reassurances) — SORTI DE 015 le 2026-08-04**, re-classé `figma-source` → 016 (reçu `dw-002-reclasse-figma-source`) : la source dessine 4 cartes FIXED de 364 avec des gaps de 32 dans un conteneur FILL de 1550, soit 1552 — **elle déborde d'elle-même de 2 px**. Figma le tolère (ni flex-shrink ni flex-basis, matrice ligne 50, CARRY-CODE-ONLY), le CSS rétrécit : d'où les −4 device px mesurés. Le contrat porte fidèlement les trois nombres ; convertir 364 en token écrirait proprement un nombre qui ne rentre pas. Conversions : DW-004 (déplacer valeur ET pointeur `/literals/` → `/tokens/` ensemble), DW-005 (minter les largeurs de cadre — doctrine déjà écrite dans `tokens/primitives.tokens.json`). Les 2 dégradés hero = **2 littéraux `background-image` à écrire, zéro code moteur** (linear-gradient est CARRY-BOTH depuis v15, `parseCssGradient` → `GRADIENT_LINEAR`). Préservation des correctifs manuels de 013 à la ré-extraction : fixture d'abord, c'est le morceau le plus risqué.
3. **016 — canvas vrai** (mutations Figma, exige le pont figma-console reconnecté) : les défauts de source (backlog 013, dont DW-003 : instance SectionHeader de FAQ figée à 50px ; **plus DW-002, arrivé de 015 le 2026-08-04** : le débordement de 2 px des 4 cartes de reassurances — trois corrections possibles, toutes dans Figma, cartes à 363,5 · conteneur à 1552 · gap à 31,33, et le choix est un arbitrage de design) corrigés DANS Figma (§VIII), puis régénération du canvas divergent — capture de TOUTES les cibles avant la première mutation (§X), preuve pixel avant/après. Déblocage Field + NavItem (défauts moteur/mesure nommés) ; **MemberCard reste bloqué honnêtement** (son plan photo est A5).
   **Contrainte ajoutée le 2026-08-03 — les photos du client.** La régénération passe par `amendSet`/`amendComponent`, qui reconstruisent les intérieurs depuis le contrat : sans la passe de récolte/restauration de `core/emit-figma-script.ts`, les vraies photos seraient détruites sur les **9 composants porteurs d'images**. La passe existe et est gardée (eval `img-paint-preserved-on-amend`), mais elle a deux limites qui visent exactement ces 9 composants : une image non replaçable part dans `unplacedImages` — **rapportée, pas restaurée** ; et l'appariement par nom puis ordre du document peut **intervertir deux photos** sans que rien ne le signale. Donc : après chaque régénération, **lire le rapport `preservedImages` / `unplacedImages` composant par composant**, et vérifier l'identité des photos, pas seulement leur présence. La capture-avant (§X) couvre la perte ; elle ne couvre pas l'interversion entre deux plans de même taille. Doctrine complète : `docs/FIGMA-CAPABILITY-MATRIX.md`, addendum du 2026-08-03.
4. **017 — images A5** (chantier moteur séparé, hors fenêtre courte) : `figma.createImage` (§ a.7 de la matrice) — la SEULE chose qui bougera realisation (99,97 %), carte (64 %), member-card (52 %), product-card (15,6 %). Jusqu'à lui, la frontière image reste une limite nommée des deux côtés.

Fait vérifié qui a cadré ce découpage : les gros écarts de parité visuelle des atomes/molécules sont **A5-image (6 lignes) et plancher renderer (15 lignes ≤3,6 %)** — la géométrie-en-tokens n'y changera rien ; ce qu'elle répare, ce sont les divergences des **organismes** (registre DW de 013). Deux populations, deux specs.

### Ce que l'exécution a corrigé dans le plan ci-dessus (2026-08-05)

- **DW-001 (logo) — la prescription du point 2 est PÉRIMÉE, et pas d'un détail.** Elle dit « une prop enum + un token dont le CHEMIN contient la prop », en s'appuyant sur l'archive demo-51. Le relevé from-dump de 015 (T048) a mesuré la prémisse au lieu de l'hériter : **il n'existe pas deux tailles**. `ds.header` porte le logo à exactement 180×34 sur ses deux variantes, `ds.footer` à 180,0985×34 — la précision flottante des vecteurs du maître, pas une intention de design. L'owner a tranché pour la voie simple : deux tokens, zéro prop, zéro lift moteur. **Ne pas construire l'infrastructure décrite au point 2** ; reçu : `specs/014-…/proofs/recus/named-repair-DW-001.md`.
- **016 a grossi.** Aux défauts de source (DW-002, DW-003) s'ajoute désormais une tâche dimensionnante : **créer les variables Figma des 83 références minées par 015**. Sans elles, l'axe `variables canvas ⟷ tokens` — celui qui doit surveiller la géométrie côté maquette — reste acquitté : `parity/baseline.json` est passé de 7 à **89 acquittements**, tous pour cette raison (reçu `figma-tokens-behind-baseline.md`). 015 a fermé l'angle mort **côté code seulement** ; c'est 016 qui rebranche l'autre moitié.
- **Le reste de la spec 015 est fait comme prévu** : box-sizing corrigé à l'émetteur (+ 3 largeurs content-box réparées à la source : sav, footer, faq), DW-004/DW-005 convertis, les 2 dégradés hero portés comme littéraux nommés déclarés, préservation des correctifs 013 gardée par un contrôle (18/18).

### À ordonnancer — trouvé, non fait, sans spec assignée

Chacun a son entrée au registre et son reçu re-testé ; aucun n'a de destination arrêtée. **C'est l'arbitrage qui reste à faire.**

| Id | Ce que c'est | Poids |
|---|---|---|
| `DW-014-001` | Le texte des `<select>` émis en enfant nu → capture vide. La surface React livrée est correcte. | Petit — **tinyspec `select-option-emit`**, décidée en 015, à lancer juste après (avant ou en parallèle de 016, sans collision : 016 ne touche pas `core/`) |
| `DW-014-002` | **La parité visuelle rend `emit-html`, jamais la surface React livrée.** Sa destination au registre disait « 015 » — 015 ne l'a pas résolu. Conséquence vivante : la seule surface que les consommateurs installent n'est mesurée par rien sur cet axe. | Moyen, et c'est le plus gênant des quatre — un angle mort d'instrument, exactement la classe de défaut que 014 existe pour refuser |
| `DW-014-003` | `texte-seo` : le titre rich-text est aplati et sa typographie n'est pas liée à travers la composition `ds.texte-seo → ds.section-header`. Seul travail de cause `contract-geometry` encore ouvert. | Moyen — demande de porter une marque rich-text ET une typographie par-disposition à travers une composition imbriquée |
| `DW-015-001` | `emit-html` accroche sa règle border-box au préfixe BEM partagé, inopérant en multi-root. **Latent** : 0 des 34 contrats n'est multi-root. | Deux lignes — appartient à la spec qui rendra Piqueray multi-root |
| — | **30 des 69 pointeurs** `/literals/` d'`audit-campaign.json` visent l'ancien emplacement après les conversions de 015 → faux négatifs « le contrat ne porte pas ce fait ». | Mécanique mais substantiel ; le fichier appartient à 013, à ne pas réécrire sans revue |
| — | **89 littéraux** de trait, peinture et typographie restent hors périmètre gouverné, donc invisibles au contrôle. La suite naturelle de 015. | Une spec entière si on la veut — même patron que 015, autre population |

Short version — four phases, each with a falsifiable exit criterion:

0. **Prove the model** — complete (July 2026): 51 contracts, two generated surfaces, three-way parity, 99/99 evals, measured governed-generation result.
1. **Harden the loop** — anatomy-level parity, fresh-file rebuild, automated visual regression.
2. **Brownfield adoption** — extract proposed contracts from *pre-existing* design + code libraries, reconcile, run diagnostic-only; public pilot on a real open-source pair.
3. **Spec candidacy** — separate format from implementation: normative spec draft, conformance kit, and a second independent implementation.
4. **Community & governance** — RFC process, engagement with DTCG / OpenUI / CEM, and a neutral multi-vendor home.
