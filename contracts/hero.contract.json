{
  "$schema": "./contract.schema.json",
  "id": "ds.hero",
  "name": "Hero",
  "version": "1.4.1",
  "description": "Piqueray Hero. Extracted from the Figma COMPONENT_SET on DS · Organisms, reviewed and adopted — not authored.\n\nv1.2.0 carries the root's photographic IMAGE fill (fills[0], imageRef b9ae58d2e309c55241eb843c1a36d90d087c1483, scaleMode FILL) as an absolutely-positioned `img` plane painted UNDER the content block — the same superposed-planes vocabulary ds.member-picture already uses, with DOM order alone deciding the stacking (no z-index channel exists, and none is needed: two positioned siblings paint in tree order). The bitmap reaches the coded surface through code-only `backgroundUrl` / `backgroundAlt` scalars (bindings.figma NONE) because `background-image: url()` is still the OPEN A5 / §a.7 gap of docs/FIGMA-CAPABILITY-MATRIX.md: the canvas side keeps the engine's generic image placeholder while the React surface renders the real pixels — the same split spec 006 documented for ds.review-card's avatarPhoto.\n\nTHOSE TWO GRADIENT_LINEAR PAINTS ARE NO LONGER LIMITS — carried since 015 (T028–T032, 2026-08-04): the root's darkening overlay (fills[2] — `linear-gradient(to top, rgba(0,0,0,0) 75%, rgba(0,0,0,0.5) 100%)`) and the Titres scrim (fills[0] — `linear-gradient(to bottom, rgba(0,0,0,0) 0%, rgba(0,0,0,0.5) 60%)`). The blocker was real and is now lifted at its root: `background-image` joined LITERAL_CHANNELS with its OWN bounded grammar (`linear-gradient(...)` only — radial/conic still refuse BY NAME, eval `gradient-literal-channel`), so the channel no longer resolves through a DTCG token reference alone. Both values are declared in `contracts/named-literals.registry.json` (receipt `hero-gradients-named-literal`) — named literals the geometry gate reads live, never invisible values: a single-use gradient token would have manufactured a fake vocabulary. The weight was measured, not guessed: the two veils darken 452 of the root's 640 px rows and accounted for 28.07 % of the master's pixels; carrying them measured 27.83 % → 10.66 % residual (receipt `hero-gradient-carry.md`, attributed cause `gradient-carry`). The remaining residual is the photo plane (A5 / §a.7), already named above. A single flat rgba() veil over the same two bands was measured too and bottomed out near 12 %, so approximating them would have both invented a Figma fact and still failed.\n\nThe instance restyle of the SectionHeader child's Titre (blanc / 54 px / 68 px / 700) is no longer a limit here: it is carried by that child contract's own `emphase: \"hero\"` axis since section-header v1.1.0.\n\nv1.4.0 (016/T042, lot B013-4): the Sous-titre paragraph stops being baked in — it rides the rich-text prop `sousTitre`, bound to the master's native TEXT property « SousTitre » (exposed by lot L-B013-4/T041; the 2026-08-05 live diagnosis showed this was the master's ONE unbound text — the title needed nothing, it was already governed through the SectionHeader instance's inherited TEXT property). Its two observed 700 ranges travel as governed segments (content.marks.strong).\n\nv1.4.1 restores the measured 1728px root width explicitly so the absolutely positioned photo plane cannot collapse the auto-layout width to its content during regeneration.",
  "semantics": {
    "element": "div",
    "provenance": "extracted"
  },
  "props": [
    {
      "name": "backgroundUrl",
      "description": "Code-only source of the root's photographic IMAGE fill (fills[0], imageRef b9ae58d2e309c55241eb843c1a36d90d087c1483). The master exposes no component property for it — A5 / §a.7 means the bitmap has no contract→canvas transport, so it rides a code-side scalar and the canvas keeps the engine's placeholder.",
      "type": "text",
      "default": "",
      "bindings": {
        "figma": {
          "kind": "NONE"
        },
        "code": {
          "prop": "backgroundUrl"
        }
      }
    },
    {
      "name": "backgroundAlt",
      "description": "Alternative text for the background photo plane. Empty by default: the Figma paint is decorative — it carries no information the surrounding copy does not already state.",
      "type": "text",
      "default": "",
      "bindings": {
        "figma": {
          "kind": "NONE"
        },
        "code": {
          "prop": "backgroundAlt"
        }
      }
    },
    {
      "name": "sousTitre",
      "description": "The hero paragraph (layer « Sous-titre », node 2111:3380) as a governed rich-text prop — 016/T042, lot B013-4: the 2026-08-05 live diagnosis showed this was the master's ONE unbound text, and lot L-B013-4 (T041) exposes the native TEXT property « SousTitre » it binds to. The two observed 700 ranges (« performance », « la solution idéale ») travel as segments; the Figma projection flattens to the property's native string value.",
      "type": "rich-text",
      "default": [
        {
          "text": "La "
        },
        {
          "text": "performance",
          "strong": true
        },
        {
          "text": " sans compromis, même en usage intensif. Atelier, bâtiment industriel, bâtiment public ou résidence : quelle que soit votre application, nous avons "
        },
        {
          "text": "la solution idéale",
          "strong": true
        },
        {
          "text": "."
        }
      ],
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "SousTitre"
        },
        "code": {
          "prop": "sousTitre"
        }
      }
    }
  ],
  "states": [],
  "anatomy": {
    "root": {
      "layout": {
        "display": "flex",
        "direction": "row",
        "justify": "space-between",
        "align": "end"
      },
      "parts": {
        "Background": {
          "description": "The root's fills[0] IMAGE paint as a photo plane. Out of flow (position: absolute) so it does not join the root's flex row; the emitter's replaced-element chrome then sizes it to the full inset box, which is the faithful statement — a Figma paint fills its NODE rather than occupying a box of its own, so no fixed 1728×640 is restated here. object-fit: cover reproduces scaleMode FILL (centered; the paint carries no imageTransform). Declared FIRST so the later positioned sibling paints over it.",
          "element": "img",
          "attrs": {
            "src": "{backgroundUrl}",
            "alt": "{backgroundAlt}"
          },
          "declared": {
            "position": "absolute",
            "object-fit": "cover"
          }
        },
        "blocTexte": {
          "layout": {
            "direction": "column",
            "align": "stretch",
            "grow": true
          },
          "tokens": {
            "gap": "{space.16}"
          },
          "declared": {
            "position": "relative"
          },
          "parts": {
            "Titres": {
              "layout": {
                "direction": "column",
                "align": "stretch"
              },
              "literals": {
                "background-image": "linear-gradient(to bottom, rgba(0,0,0,0) 0%, rgba(0,0,0,0.5) 60%)"
              },
              "tokens": {
                "gap": "{space.16}",
                "padding-top": "{space.96}",
                "padding-right": "{space.89}",
                "padding-bottom": "{space.48}",
                "padding-left": "{space.89}"
              },
              "parts": {
                "SectionHeader": {
                  "component": {
                    "id": "ds.section-header",
                    "props": {
                      "titre": [
                        {
                          "text": "Portes de garage",
                          "strong": true
                        },
                        {
                          "text": " industrielles"
                        }
                      ],
                      "accroche": "Plus de 50 ans d’expérience",
                      "accroche2": false,
                      "disposition": "standard",
                      "emphase": "hero",
                      "alignement": "gauche"
                    }
                  }
                },
                "wrapper": {
                  "layout": {
                    "direction": "row",
                    "justify": "center",
                    "align": "end"
                  },
                  "tokens": {
                    "gap": "{space.32}"
                  },
                  "parts": {
                    "sousTitre": {
                      "description": "UNE LIMITE LEVÉE, UNE LIMITE VIVE sur cette part.\n\n(1) LEVÉE (016/T042, lot B013-4) — LE GRAS VOYAGE DÉSORMAIS PAR LA PROP `sousTitre` (content.prop → sousTitre, gras gouverné par content.marks.strong), liée à la propriété Texte native « SousTitre » que le lot L-B013-4 (T041) expose sur le master. Diagnostic vif 2026-08-05 (registre B013-4) : ce Sous-titre était le SEUL texte non lié du master — les 3 autres textes passent par les propriétés héritées des instances (SectionHeader, Bouton). État antérieur, gardé pour mémoire : GRAS NON PORTÉ — Figma met deux plages en 700 sur le node 2111:3380 (`styleOverrideTable` clé 16 = Montserrat-Bold ; `characterStyleOverrides` 3→13 = « performance », 162→179 = « la solution idéale »), sur une base Montserrat 400. Le seul vocabulaire de texte riche du schéma est `Part.content`, un `strictObject` dont le champ `prop` est OBLIGATOIRE : le gras ne peut donc voyager qu'à travers une prop rich-text, et une prop rich-text exige une liaison TEXT native — or le master Hero n'exposait alors AUCUNE propriété de composant propre (`componentPropertyDefinitions` absent, fait déjà acquitté par hero.property.aucune-propriete-exposee). Inventer une liaison TEXT pour faire passer le gras aurait fabriqué une propriété Figma inexistante : la part restait donc en `text` littéral, chaîne exacte du census, le gras porté statiquement (textSegments, v19). LE CORRECTIF DE FOND ANNONCÉ ICI EST CELUI APPLIQUÉ : la propriété Texte exposée sur le master (T041), le gras redevenu plage gouvernée portée par la prop (T042) — jamais un contournement en code.\n\n(2) LARGEUR LITTÉRALE MESURÉE, pas une grille. Figma dit FILL (layoutGrow=1) et calcule 1164 = 1550 (wrapper FILL) − 32 (itemSpacing) − 354 (Bouton HUG) ; le census confirme la boîte à 1164 px. `layout.grow` émet `flex: 1 1 auto`, dont la base reste le CONTENU (jamais `flex-basis: 0`), donc la base dépasse le conteneur et le retrait négatif se répartit sur les DEUX enfants : le Bouton se faisait comprimer sous sa largeur HUG et son libellé passait sur deux lignes. Le littéral fixe la base à la valeur observée ; `flex-grow: 1` reste actif, donc la sémantique FILL survit vers le haut. Même technique et même reçu que `literals.width: \"282px\"` sur la racine de ds.review-card (spec 006, decisions.md § contournement vérifié par la prior-art rule) et que ds.avatar-group dans l'archive demo-51. À RECALCULER si l'itemSpacing du wrapper, le padding de Titres ou la largeur HUG du Bouton changent. Le vrai correctif est moteur (une base FILL qui n'est pas le contenu) et reste au backlog — `core/` non modifié, Constitution VII.",
                      "layout": {
                        "grow": true
                      },
                      "tokens": {
                        "width": "{size.hero.sous-titre}",
                        "color": "{color.blanc}",
                        "font-size": "{font.size.24}",
                        "line-height": "{font.line-height.32}"
                      },
                      "content": {
                        "prop": "sousTitre",
                        "marks": {
                          "strong": "{font.weight.bold}"
                        }
                      }
                    },
                    "Bouton": {
                      "component": {
                        "id": "ds.button",
                        "text": "Demander un devis gratuit",
                        "props": {
                          "variant": "outlineBlanc",
                          "iconRight": true
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      },
      "tokens": {
        "font-family": "{font.family.montserrat}",
        "height": "{size.hero.root}",
        "gap": "{space.10}"
      },
      "literals": {
        "width": "1728px",
        "background-image": "linear-gradient(to top, rgba(0,0,0,0) 75%, rgba(0,0,0,0.5) 100%)"
      },
      "declared": {
        "position": "relative"
      }
    }
  },
  "anchors": {
    "figma": {
      "fileKey": "d9FYAUcqdcNtsuaMgLefvJ",
      "componentSetKey": "75f3b0746cdf57be051fb4cfc388588b5e03484a",
      "nodeId": "2111:3382",
      "dumpedAt": "2026-07-27"
    },
    "code": {
      "importPath": "src/components/Hero",
      "export": "Hero"
    }
  },
  "category": "section"
}
