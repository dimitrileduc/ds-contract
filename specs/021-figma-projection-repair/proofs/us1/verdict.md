# Verdict US1 — restaurations visuelles

Verdict : **vert** sur `DS · Organisms` au pin Figma `2385797321547430456`.

- **Hero** — maître et huit occurrences à `1728×640`; plan photo `1728×640`; huit photos d'instance distinctes; titre, sous-titre et CTA visibles. L'amend généré a conservé les neuf images avec un verdict photo vert.
- **SAV** — maître amendé en place; quatre images conservées; rendu déjà accepté par l'owner.
- **Catégories principales** — trois cartes liées de `474px` en `x=89/627/1165`; images ramenées à `266.665px` par proportion native au lieu de rester à `418px`; contenu et photos conservés.
- **Réalisations** — bloc `1319px` en `x=204.5`; `SectionHeader` `628px`; wrapper en `x=692`; grille intacte en `x=89`, `y=276`.

La capture finale contient 72 artefacts, 60 empreintes image et 155 liens d'instance. Les preuves visuelles se trouvent dans `specs/021-figma-projection-repair/proofs/after/` et le détail des mutations dans `proofs/us1/apply-receipt.json`.
