{
  "schemaVersion": 1,
  "id": "retired-switch",
  "claim": "the 'switch' TRIAGE rule (originalClass 'renderer') can never fire again: ds.switch was removed at the Piqueray reconversion, and no contract, no parity subject (live or quarantined) exists for it.",
  "claimSource": "extract/figma/visual-parity/triage.ts RETIRED_RULES entry 'switch' (cause-vocabulary.md §4.2)",
  "method": "ls contracts/*.contract.json | xargs -n1 basename | grep -i switch ; grep -oE \"id: '[a-z-]+'\" extract/figma/visual-parity/subjects.ts | sort -u | grep -i switch ; grep -i switch catalog/catalog.json",
  "observed": {
    "contractsMatch": "none — no contracts/switch.contract.json, no *switch* file under contracts/",
    "subjectsMatch": "none — 'switch' is not an id in PARITY_SUBJECTS nor LEGACY_SUBJECTS (extract/figma/visual-parity/subjects.ts)",
    "catalogMatch": "none — catalog/catalog.json carries no switch entry"
  },
  "verdict": "confirmed",
  "browser": null,
  "date": "2026-08-03"
}
