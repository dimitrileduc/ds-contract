{
  "schemaVersion": 1,
  "id": "retired-badge",
  "claim": "the 'badge' TRIAGE rule (originalClass 'renderer') can never fire again: ds.badge was removed at the Piqueray reconversion, and no contract, no parity subject (live or quarantined) exists for it.",
  "claimSource": "extract/figma/visual-parity/triage.ts RETIRED_RULES entry 'badge' (cause-vocabulary.md §4.2)",
  "method": "ls contracts/*.contract.json | xargs -n1 basename | grep -i badge ; grep -oE \"id: '[a-z-]+'\" extract/figma/visual-parity/subjects.ts | sort -u | grep -i badge ; grep -i badge catalog/catalog.json",
  "observed": {
    "contractsMatch": "none — no contracts/badge.contract.json, no *badge* file under contracts/",
    "subjectsMatch": "none — 'badge' is not an id in PARITY_SUBJECTS nor LEGACY_SUBJECTS (extract/figma/visual-parity/subjects.ts)",
    "catalogMatch": "none — catalog/catalog.json carries no badge entry"
  },
  "verdict": "confirmed",
  "browser": null,
  "date": "2026-08-03"
}
