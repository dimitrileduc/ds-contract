# Audit et réparation — HeroVideo

Verdict runner final : **vert**.

Rapport mécanique : `specs/component-repairs/hero-video/run-001/audit.json`.

## État live contrôlé après réparation

- version réauditée : `2386491573484816096` ;
- master historique unique conservé : `HeroVideo`, nœud `2151:5552` ;
- Container local unique : `Container · HeroVideo`, nœud `2448:4731` ;
- Container horizontal auto-layout, référence `1728×720` ;
- master en sizing horizontal `FILL` ;
- contrôle réduit réel à 1440, sans débordement ;
- aucune écriture de Page.

## Titre

Le titre n'a pas perdu de rich text : il n'en a jamais eu. Les preuves
historiques le décrivent comme un unique texte lié au Text Style
`Titre Hero vidéo`, Montserrat Regular 44/48.

L'état live est conforme :

- `HeroVideo/Accroche` → `named-exact`, style `Titre Hero vidéo` ;
- `Button/label` → `named-exact`, style `Libellé bouton` ;
- aucun range rich text ;
- aucun texte custom injustifié.

## Changements appliqués

1. présenter le master existant dans un unique Container local auto-layout et
   passer son sizing horizontal à `FILL`, sans dupliquer ni reconstruire le
   master ;
2. déclarer la dépendance directe stable `ds.button` dans le manifeste.

Les largeurs restent propres à HeroVideo : référence actuelle 1728 et contrôle
réduit déclaré 1440. Cette paire n'est pas généralisée aux autres organismes.

## Gates obtenus

- master, image, gradients, contenu, styles de texte, instances et overrides
  protégés : zéro différence inattendue ;
- capture master et usage Page avant/après : rendu inchangé à 1728 ;
- deux captures 1440 byte-identiques ;
- seconde application : `no-op`, aucun nœud créé ou modifié ;
- réaudit final : zéro finding et zéro changement proposé ;
- typecheck et fixtures du workflow : verts.

Le workflow reste un runner documenté. Aucune skill Codex n'a été créée.
