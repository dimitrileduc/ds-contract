# Audit read-only — Hero

Verdict : **green — aucun changement proposé**.

Cet audit teste la phase initiale de
`docs/internal/component-repair-workflow.md`. Il ne lance aucun script Figma,
ne modifie aucun nœud et n'ouvre pas une nouvelle campagne d'application.

## Périmètre

- master : `Hero`, nœud `2111:3382` ;
- dépendances structurelles : `SectionHeader` et `Button` ;
- usages : les huit instances Page inventoriées dans
  `specs/021-figma-projection-repair/proofs/hero-page-copy-recovery.json` ;
- contexte seulement : le Header superposé au Hero ;
- explicitement hors périmètre d'écriture : `Header`, `NavItem` et tous les
  nœuds de Page.

## Classification des textes

| Nœud | Classe | Justification |
| --- | --- | --- |
| `SectionHeader/Accroche` | `named-exact` | Text Style historique `Accroche`, définition exacte 20/25 Regular avec tracking gouverné. |
| `SectionHeader/Titre` | `rich-ranges` | Le copy varie par Page et conserve ses plages Light/Regular/Bold ; aucun Text Style global n'est attendu. |
| `Hero/sousTitre` | `rich-ranges` | Prop native `SousTitre` avec plages Regular/Bold préservées ; aucun Text Style global n'est attendu. |
| `Button/Libellé` | `named-exact` | Text Style historique `Libellé bouton`, définition exacte 16/22 Medium uppercase. |

Aucun `historical-custom` n'est requis dans ce périmètre et aucun texte brut
injustifié n'est détecté. Le Header/NavItem n'est pas reclassé comme dépendance
du Hero du seul fait qu'il apparaît dans sa capture Page.

## Faits protégés contrôlés

- identité du master `2111:3382` conservée ;
- aucune variante de largeur créée ;
- photo et deux voiles du Hero présents dans le contrat :
  `VoileNavigation` en haut et scrim `Titres` en bas ;
- copy et plages rich text des huit Pages conservés par la preuve de recovery ;
- liens `SectionHeader` et `Button`, propriétés et overrides conservés dans les
  captures before/after/idempotence de la campagne 021 ;
- aucun nœud Page autorisé à l'écriture ;
- le contrôle source ciblé Text Styles passe avec 18 recettes historiques,
  `62 linked / 11 historical custom / 21 rich` et une seconde application de
  tokens qui conserve les ids.

## Sources

- `contracts/hero.contract.json`
- `tokens/semantic.tokens.json`
- `evals/fixtures/figma-text-styles-piqueray.expected.json`
- `specs/007-figma-extractable-source/releves/notes-post-L1-2026-07-26.json`
- `specs/021-figma-projection-repair/proofs/hero-page-copy-recovery.json`
- `specs/021-figma-projection-repair/proofs/before/hero_master.structure.json`
- `specs/021-figma-projection-repair/proofs/after/hero_master.structure.json`
- `specs/021-figma-projection-repair/proofs/idempotence/hero_master.structure.json`

## Gate

Le verdict `green` termine l'exécution : pas de snapshot supplémentaire, pas de
proposition, pas de génération, pas d'application Figma et pas de nouvelle
version du fichier.
