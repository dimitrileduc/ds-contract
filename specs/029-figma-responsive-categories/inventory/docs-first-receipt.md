# Docs-first receipt — feature 029

Opened: 2026-08-26

## Authorities read in constitutional order

1. `.specify/memory/constitution.md` — version 1.2.0, especially Principles II,
   III, V, VII, VIII, IX, X and XI plus Worktree Gate F1.
2. `docs/internal/component-repair-workflow.md` — runner v2 sequence, single-target
   boundary, audit/read-only gate, snapshot/capture/apply/no-op protocol and stable
   refusal diagnostics.
3. `docs/responsive-figma.md` — Auto Layout first, strict Grid versus Wrap,
   explicit state selection, and the absence of automatic breakpoints in Figma
   Design.
4. `docs/FIGMA-CAPABILITY-MATRIX.md` — FIXED/HUG/FILL semantics, native Grid,
   bindable spacing/dimension fields, typography bounds, media preservation risks
   and named limits.
5. Feature 029 artifacts — `spec.md`, `plan.md`, `research.md`, `data-model.md`,
   `quickstart.md`, all three internal contracts and `tasks.md`.
6. Template and handoff 028 — `plan.md`, `quickstart.md`, `decisions/README.md`
   and the three files under `handoff/`.
7. Historical 023 evidence — all four audit notes and owner gates A/B/C.
8. Historical 021 evidence — the CategoriesPrincipales owner decision, accepted
   receipt and campaign closure summary.

## Settled constraints carried into implementation

- The active Superset workspace is the implementation target; no extra worktree.
- Two fresh single-component campaigns are sequenced card then section, inside one
  global capture/verification cycle.
- All seven Page usages are addressed by position and remain read-only.
- The card is mutable only after a fresh whole-file exclusivity proof.
- Every other shared child is protected; the section campaign permits no direct
  child write, including card instances.
- Existing-set topology may truthfully create zero nodes; creations and propagated
  deltas are never normalized away.
- Style×Colonnes scenario selection is explicit. Resizing alone is not described as
  selecting a Figma state.
- Auto Layout/internal adaptation is demonstrated before explicit state creation.
  Strict columns use Grid where the accepted last-row behavior requires it; Wrap is
  not silently treated as an equivalent grid.
- Spacing/dimension changes bind to existing primitives only. Local typography is
  restricted to font size, line height and horizontal alignment and remains
  `pending-responsive-text-style`.
- Captures of both masters and all seven usages must be verified before the first
  live write. Page writes are always forbidden.
- Fixtures and registered evals precede runner capability claims and any live use.
- H1–H4 are real owner gates. Mechanical proof never fabricates approval.
- Closure remains `figma-ahead/pending-home-responsive-promotion`; contracts, code,
  HTML and Odoo are outside this feature.

## Historical baseline (not current authority)

- 023 observed set `2115:4277` with four Style×Colonnes members, card set
  `2495:6770`, and seven usages at `2115:4392`, `2115:4278`, `2115:4438`,
  `2115:4297`, `2115:4411`, `2115:4324`, `2115:4364`.
- 023 records six 2-column usages, one 3-column usage, two superposed and five
  stacked; its current-card/source IDs must be re-measured because earlier notes
  also cite pre-cleanup `Carte/Categorie` `2063:1611`.
- 023 already experienced image loss during a component swap and recovered it from
  the before version. This reinforces full before-capture and media-fact protection.
- 021 accepted the CategoriesPrincipales visual repair with zero unexpected diff
  and a strict no-op second reconstruction. That appearance is a comparison input,
  never a fresh-audit substitute.

## Closure status

Open. T062 must revisit this receipt against the dated 028-deviation register and
final handoff before the feature can be declared closed.
