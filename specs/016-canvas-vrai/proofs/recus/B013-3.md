# Reçu B013-3 — SectionHeader : l'emphase typographique devient une variante gouvernée

**Date** : 2026-08-06 · **Lot planifié** : `L-B013-3` (résolu par la vague de
régénération O-12, pas par un lot dédié — même mouvement que B013-2) · **Verdict : ✅ clos**
**Journal** : `specs/016-canvas-vrai/decisions.md` O-12 (gain d'axe) et O-15 (FINAL17)
**Commit porteur de l'élévation** : `e856844` (contrat v2.1.0) · description toilettée : `0ed9981` (T050)
**Recensement de limite levée** : `proofs/recus/limites-levees.md` §1.1

---

## Le défaut, re-relevé au vif (T026, 2026-08-05) — cinq cas là où la mémoire en nommait trois

Méthode : `figma_execute` LECTURE SEULE — toutes les instances du set SectionHeader
(`2090:2397`) résolues par `getMainComponentAsync`, scannées par POSITION, typo du
`Titre` relevée par signature `taille/interligne/graisse/couleur`. Détail :
`registre/defauts-source.json`, entrée B013-3, `diagnosticVif`.

Référence master : **40/50/Regular/#26282C**. Signatures relevées :

| Signature | Instances | Exemple d'usage |
|---|---:|---|
| 40/50/Regular/#26282C (= référence) | 31 | — |
| 54/68/(mixed)/#FFFFFF | 9 | Titres (hero) |
| 24/30/(mixed)/#26282C | 9 | h2 (texte-seo) |
| 32/40/(mixed)/#26282C | 4 | Presentation |
| 32/40/Regular/#26282C | 3 | ProduitsECommerce |
| 40/50/(mixed)/#26282C | 3 | Bloc en-tête |

**25 instances en écart avec le master.** La mémoire (2026-07-30) nommait 3 cas
(hero 54/68/700/blanc ; presentation 32/40 ; texte-seo 24/30) ; le vif en a montré
**cinq** hors référence — s'ajoutent ProduitsECommerce (32/40 Regular, distinct de
Presentation) et Bloc en-tête (40/50 à poids mixte). Le poids remonte souvent `mixed`
(plusieurs styles dans un même bloc), ce que le diagnostic d'origine ne disait pas.

## La correction — par élévation VARIANT, pas par le geste planifié

- **O-12 (2026-08-06)** : le SET `2090:2397` **gagne la dimension `Emphase`** (avec
  `Alignement`, même vague — classe moteur n°6 : un SET qui gagne une dimension
  complète les noms de ses variantes existantes aux défauts du spec et fusionne avec
  la jumelle fraîche, le bloc tournant AVANT la première lecture de
  `componentPropertyDefinitions`). « SET 18→16 variantes, sain », **16 variantes
  générées mécaniquement**, posées master par master.
- **« C'était LA typo signalée par l'owner »** (O-12 : « la régression c'est la
  typo ») : le TexteSEO composait `emphase: compact` que le canvas ne savait pas
  recevoir — après élévation, **TexteSEO 403→383 = origine** (titre 24px).
- **O-13** : le hero « noir » signalé en revue était déjà réparé par l'élévation
  `Emphase=Hero` (titre vif 255,255,255) — le cas hero 54/68/blanc du diagnostic est
  couvert par la même variante.
- Les valeurs typographiques portées par le contrat (54/68, 32/40, 24/30) sont celles
  **re-relevées au vif T026**, jamais recopiées de la mémoire (exigence du plan).

## La promotion code-side — vérifiée au vif du 2026-08-06

`contracts/section-header.contract.json` **v2.1.0** (semver mineur, §VI —
`registre-source.md` : « axe code-side → binding VARIANT = mineur ») :

```json
"emphase": { "bindings": { "figma": { "kind": "VARIANT", "property": "Emphase",
  "values": { "standard": "Standard", "hero": "Hero", "moyen": "Moyen", "compact": "Compact" } } } }
```

La prop cesse d'être un axe code-side (`kind: NONE`) ; l'ancienne « LIMITE NOMMÉE » de
la description est réécrite en « LIMITE LEVÉE » (histoire conservée), et le modèle du
poids hero — base 300 littérale, plage marquée `content.marks.strong` — toujours vrai,
est gardé tel quel (`limites-levees.md` §1.1, commit `0ed9981`).

## Annonce ⟷ observé (pixel)

**Annonce** : rendu inchangé, gouvernance changée.

**Observé** : la résolution étant passée par la vague de régénération, son compte est
celui des vagues journalisées — O-12 : TexteSEO **403→383 = origine** (le seul écart
typo mesuré nommément) ; puis **O-15 / FINAL17 vs référence pré-chantier** : 4 pages
exactes, « il ne reste AUCUN défaut non attribué » — aucun delta restant n'a l'emphase
pour cause. `npm run parity` : **exit 0** après ré-extraction des clichés (O-15/O-16) —
le finding de promotion du binding VARIANT est résolu, pas acquitté.

## Re-vérification

```bash
# côté contrat — binding VARIANT réel :
python3 -c "import json; d=json.load(open('contracts/section-header.contract.json')); \
p=[x for x in d['props'] if x['name']=='emphase'][0]; b=p['bindings']['figma']; \
print(d['version'], b['kind'], b['property'], sorted(b['values'].values()))"
# attendu : 2.1.0 VARIANT Emphase ['Compact', 'Hero', 'Moyen', 'Standard']
npm run parity   # attendu : exit 0 (aucun finding sur l'axe Emphase)
```

```js
// figma_execute, lecture seule — le SET porte l'axe :
await figma.loadAllPagesAsync();
const set = await figma.getNodeByIdAsync('2090:2397');
console.log(Object.keys(set.variantGroupProperties));       // contient 'Emphase' (et 'Alignement')
console.log(set.variantGroupProperties['Emphase'].values);  // ['Standard','Hero','Moyen','Compact']
console.log(set.children.length);                           // 16
```

Aucun re-relevé canvas n'a été fait pour CE reçu (fenêtre sans pont) : les faits canvas
cités sont ceux des journaux O-12/O-13/O-15 et du `diagnosticVif` du 2026-08-05,
sourcés ligne par ligne.
