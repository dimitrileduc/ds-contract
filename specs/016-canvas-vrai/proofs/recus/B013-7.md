# Reçu B013-7 — `hero` : le dégradé mort supprimé du master

**Date** : 2026-08-06 · **Lot** : `L-B013-7` (workflow multi-agents, agent canvas séquentiel §XI)
**Verdict : ✅ clos** · **Point de restauration** : version Figma nommée `016/B013-lots/avant`
**Journal** : `specs/016-canvas-vrai/decisions.md` O-16 · **Commit** : `0ed9981`

---

## Le défaut, re-relevé au vif (T026, 2026-08-05) — la mémoire confirmée à l'identique

Méthode : `figma_execute` LECTURE SEULE — `fills` du root du master `Hero`
(`2111:3382`), lus par index (`registre/defauts-source.json`, entrée B013-7,
`diagnosticVif`).

| Index | Type | visible |
|---:|---|---|
| 0 | IMAGE (la photo) | true |
| **1** | **GRADIENT_LINEAR** | **false** ← le mort |
| 2 | GRADIENT_LINEAR (le voile assombrissant) | true |

Écart avec le diagnostic d'origine (mémoire `figma-cleanup-backlog-013`, 2026-07-30) :
**aucun**. Piège nommé au relevé et respecté au geste : le root porte un **second
dégradé VISIBLE** (`fills[2]`) à ne pas confondre avec le mort.

## Le geste (2026-08-06, nuit — journal O-16)

> « **B013-7** : le fills[1] GRADIENT visible:false du master hero supprimé. »

## Annonce ⟷ observé — le cycle pixel global du lot (§XI)

**Annonce** : ZÉRO écart visuel — un fill invisible ne peint pas ; tout pixel qui bouge
annule le lot ET révèle que le diagnostic était faux.

**Observé** (cycle pixel global unique autour du batch B013-1/5/7 + marqueurs, tenu par
l'orchestrateur — `.page-parity/FINAL18/verdict-vs-f17/verdict.md`) : 6/9 maquettes
**byte-identiques** (dont accueil, la porteuse du hero d'accueil, à **0 pixel**) ; les
3 micro-diffs (a-propos 31 030 px, depannage-sav 11 776 px, contactez-nous 1 658 px)
sont jugés au journal « anti-aliasing de re-rendu (contenu identique à l'œil) », et vs
référence pré-chantier : « rien de dégradé, a-propos AMÉLIORÉ (189k→175k) ». Le
diagnostic tenait : rien de ce qui a bougé n'a la forme d'un dégradé plein-cadre.

## Contrepartie code : aucune — et c'est vérifié, pas supposé

`promotionCodeSide: null`. Vérifié au vif du 2026-08-06 sur
`contracts/hero.contract.json` (v1.3.0) : le contrat porte exactement les **deux
paints visibles** du root — la photo (`fills[0]`, plan `img` code-only
`backgroundUrl`, limite A5/§a.7) et le voile (`background-image:
linear-gradient(to top, rgba(0,0,0,0) 75%, rgba(0,0,0,0.5) 100%)`, littéral nommé du
registre `named-literals` depuis 015). Le dégradé mort n'a **jamais** été porté par
aucun canal — après suppression, le canvas (2 fills, tous visibles) correspond 1:1 au
modèle du contrat ; la correspondance s'est **améliorée**, pas seulement conservée.

**Détail nommé, pas tu** : la description du contrat cite historiquement le voile comme
« fills[2] » (indexation du dump d'origine) ; après suppression du mort, ce voile vit à
l'index 1. Référence d'histoire dans une prose, pas un canal fonctionnel (le canal
porté est `background-image`, sans index) — nommée ici, pas corrigée en douce (édition
de contrat hors périmètre du lot).

## Re-vérification

```js
// figma_execute, lecture seule — plus aucun fill invisible :
await figma.loadAllPagesAsync();
const hero = await figma.getNodeByIdAsync('2111:3382');
console.log(hero.fills.map((f, i) => ({ i, type: f.type, visible: f.visible !== false })));
// attendu : 2 fills — [{i:0, type:'IMAGE', visible:true}, {i:1, type:'GRADIENT_LINEAR', visible:true}]
// tout fill à visible:false ferait échouer la re-vérification
```

Aucun re-relevé canvas n'a été fait pour CE reçu (fenêtre sans pont) : les faits canvas
cités sont ceux du journal O-16 et du `diagnosticVif` du 2026-08-05, sourcés ligne par
ligne. **Restauration** si besoin : historique de versions Figma → `016/B013-lots/avant`.
