# Reçu B013-2 — SectionHeader : l'alignement devient une variante gouvernée

**Date** : 2026-08-06 · **Lot planifié** : `L-B013-2` (résolu par la vague de
régénération O-12/O-14, pas par un lot dédié) · **Verdict : ✅ clos**
**Journal** : `specs/016-canvas-vrai/decisions.md` O-12 (gain d'axe) et O-14 (reposes)
**Recensement de limite levée** : `proofs/recus/limites-levees.md` §1.2

---

## Le défaut, re-relevé au vif (T026, 2026-08-05) — 7× plus large que la mémoire

Méthode : `figma_execute` LECTURE SEULE — toutes les instances du set SectionHeader
(`2090:2397`) résolues par `getMainComponentAsync`, scannées par POSITION, jamais par
nom (§VIII). Détail : `registre/defauts-source.json`, entrée B013-2, `diagnosticVif`.

| Fait | Mémoire (2026-07-30) | Relevé vif (2026-08-05) |
|---|---|---|
| Master de référence | 2090:2385 centre (CENTER) | confirmé : `alignementTitre: CENTER` |
| Surcharges `textAlignHorizontal=LEFT` d'instance | « 5 usages sur 7 » | **34 instances sur 59** (LEFT 34 / CENTER 25) |

Le vif a montré une ampleur **7× supérieure** au diagnostic d'origine — c'est le
rendement de la règle « une décision du dépôt n'est pas un fait ».

## La correction — par élévation VARIANT, pas par le geste planifié

- **O-12 (2026-08-06)** : le SET `2090:2397` **gagne la dimension `Alignement`**
  (généralisation du rename State — classe moteur n°6 : un SET qui gagne une dimension
  complète les noms de ses variantes existantes aux défauts du spec et fusionne avec la
  jumelle fraîche, le bloc tournant AVANT la première lecture de
  `componentPropertyDefinitions`). « SET 18→16 variantes, sain. »
- **O-14 (2026-08-06)** : les alignements d'**usage** re-posés depuis le dump REST
  versionné (`version=2384251202054787848`, l'état pré-chantier intégral) —
  « **114 gestes `textAlignHorizontal`** » appariés (hostId, nomText, occurrence),
  dans le lot des 198 gestes rich-text + alignements. Mesure FINAL15→FINAL16 :
  a-propos 419k→192k, dépannage 384k→157k, motorisation 356k→129k,
  portes-de-garage 441k→213k px.
- **O-15 (FINAL17)** : plus aucun défaut non attribué — chaque delta restant a un nom,
  une cause et un statut.

## La promotion code-side — vérifiée au vif du 2026-08-06

`contracts/section-header.contract.json` **v2.1.0** (semver mineur, §VI) :

```json
"alignement": { "bindings": { "figma": { "kind": "VARIANT", "property": "Alignement",
  "values": { "centre": "Centre", "gauche": "Gauche" } } } }
```

La prop cesse d'être un axe code-side (`kind: NONE`) ; l'ancienne « LIMITE NOMMÉE » de
la description est réécrite en « Axe gouverné […] LIMITE LEVÉE » avec le census 013
(5/7) ET le relevé vif 016 (34/59) conservés comme histoire (commit `0ed9981`).
Build + re-pins golden/engine.receipt verts (tasks T038, journal O-16 : gates).

## Annonce ⟷ observé (pixel)

**Annonce** : rendu inchangé — la variante reproduit l'état actuel de chaque usage ;
seule la gouvernance change.

**Observé** : la résolution étant passée par la vague de régénération, son compte pixel
est celui des vagues journalisées — FINAL15→FINAL16 (chute massive du diff après repose
des 114 alignements, chiffres ci-dessus) puis **FINAL17 vs référence pré-chantier**
(O-15) : 4 pages exactes, chaque delta restant nommé et assumé (aucun n'a l'alignement
pour cause). `npm run parity` : **exit 0** après ré-extraction des clichés (O-15/O-16) —
le finding de promotion `figma|*` du binding VARIANT est résolu, pas acquitté.

## Re-vérification

```bash
# côté contrat — binding VARIANT réel :
python3 -c "import json; d=json.load(open('contracts/section-header.contract.json')); \
p=[x for x in d['props'] if x['name']=='alignement'][0]; \
print(d['version'], p['bindings']['figma']['kind'], p['bindings']['figma']['property'])"
# attendu : 2.1.0 VARIANT Alignement
npm run parity   # attendu : exit 0 (aucun finding sur l'axe Alignement)
```

```js
// figma_execute, lecture seule — le SET porte l'axe :
await figma.loadAllPagesAsync();
const set = await figma.getNodeByIdAsync('2090:2397');
console.log(Object.keys(set.variantGroupProperties));      // contient 'Alignement' (et 'Emphase')
console.log(set.variantGroupProperties['Alignement'].values); // ['Centre','Gauche']
console.log(set.children.length);                           // 16
```

Aucun re-relevé canvas n'a été fait pour CE reçu (fenêtre sans pont) : les faits canvas
cités sont ceux des journaux O-12/O-14/O-15 et du `diagnosticVif` du 2026-08-05.
