# Reçu B013-8 — `outilneNoir` : clos SANS geste canvas, bump MAJEUR côté code

**Date** : 2026-08-06 · **Lot** : `L-B013-8` (workflow multi-agents — agents fichiers,
AUCUN geste canvas) · **Verdict : ✅ clos**
**Journal** : `specs/016-canvas-vrai/decisions.md` O-16 · **Commit** : `0ed9981`

---

## Le défaut, re-relevé au vif (T026, 2026-08-05) — la source était propre

Méthode : `figma_execute` LECTURE SEULE — axe `Style` du set Bouton (`6:122`) + scan
de TOUTES les instances pour la valeur fautive (`registre/defauts-source.json`,
entrée B013-8, `diagnosticVif`).

| Fait | Mémoire (2026-07-30) | Relevé vif (2026-08-05) |
|---|---|---|
| Valeur d'enum côté Figma | « outilneNoir VENUE DE FIGMA — renommer côté canvas » | la variante s'appelle « **Outline noir** », correctement orthographiée |
| Usages de la valeur fautive dans Figma | (supposés) | **0** |

**CÔTÉ FIGMA, LE DÉFAUT N'EXISTAIT PLUS** (la régénération l'avait déjà purgé — O-16 :
« constat »). La faute vivait ailleurs : (1) la **clé d'enum** `outilneNoir` du contrat
`ds.button` v1.6.0 ; (2) le **sujet d'instrument** `Property 1=Outilne noir` de
`extract/figma/visual-parity/subjects.ts` — une propriété ET une valeur qui
n'existaient plus (d'où le `SKIPPED` du relevé T007, couverture de mesure perdue).
C'est le cas d'école de la règle « une décision du dépôt n'est pas un fait » —
application de T026 : *tout défaut qui ne se reproduit plus est clos sans geste*.

## Le geste : promotion code-side MAJEURE (aucun pixel, aucun canvas)

**`contracts/button.contract.json` 1.6.0 → 2.0.0** (§VI : une valeur d'enum renommée
est un breaking change) — la clé `outilneNoir` → `outlineNoir` dans `type.enum` ET
dans `bindings.figma.values` (→ « Outline noir », inchangé côté Figma). Les
**7 sources migrées dans le même mouvement** (diff `0ed9981`, vérifié ligne à ligne) :

| # | Source | Ce qui portait la faute |
|---|---|---|
| 1 | `contracts/button.contract.json` | la clé d'enum + le binding |
| 2 | `contracts/faq.contract.json` | `variant: "outilneNoir"` d'une part composée + description |
| 3 | `contracts/reassurances.contract.json` | `variant: "outilneNoir"` ×2 (parts composées) |
| 4 | `contracts/section-header.contract.json` | `variant: "outilneNoir"` (part composée Avec CTA) |
| 5 | `evals/harness.ts` | `VARIANT_DECL` (la déclaration attendue par les évals) |
| 6 | `evals/run.ts` | la mutation C2 `replaceInFile` ciblant la clé |
| 7 | `evals/adherence/ARCHIVE.md` | la liste d'enum citée |

**Générés retombés au build** (jamais édités à la main) : `src/components/Button/*`,
`figma-sync/batch-01.js` + re-pins `evals/golden.json`, `figma-sync/plugin/engine.receipt.json`,
`parity/report.json`. **Gates au commit** (O-16) : eval **184/184**, parity **exit 0**,
geometry 0.

## L'instrument re-pointé — la couverture perdue restaurée

`extract/figma/visual-parity/subjects.ts` : le sujet button-with-icons pointe désormais
`variantName: 'Style=Outline noir'` (le sujet suit le canvas VIVANT ; l'ancienne
graphie et l'axe `Property 1` disparu sont documentés en commentaire). Conséquence au
gate (note T035, `proofs/measure-gate-post-dw.txt`) : `rows.json` du run frais compte
**0 ligne figma-source** — les « figma-source=2 » du résumé comptent des RÈGLES de
triage, pas des lignes (décalage d'instrument consigné, pas maquillé). Les 2
« figma-source » du gate étaient **l'instrument en retard sur la source réparée, pas
le fichier** (O-16).

## Annonce ⟷ observé

**Annonce** : rendu inchangé, nom changé. **Observé** : aucun geste canvas, donc aucun
pixel à mesurer pour CE lot (le cycle global FINAL18 du batch couvre les lots canvas
B013-1/5/7) ; côté code, le delta git est exactement le renommage + les régénérations,
et les gates sont verts (eval 184/184, parity 0).

## Résidus nommés — l'ancienne graphie qui reste, et pourquoi

`git grep -l outilneNoir` sur le versionné, après migration :

| Où | Nature | Statut |
|---|---|---|
| `catalog/catalog.json`, `catalog/components/button.json` | **générés** (interdits d'édition manuelle), encore v1.6.0/`outilneNoir` — la régénération du catalog (`npm run catalog`) n'a pas été relancée par le workflow (build interdit dans cette fenêtre) | **résidu réel, à résorber au prochain build catalog** |
| `core/samples/*` (~24 fichiers) | échantillons d'extraction **figés** (entrées de test historiques ; evals 184/184 verts avec eux tels quels) | délibérément non migrés |
| `dashboard/src/views/Governance.tsx` | un commentaire citant l'ancien enum | prose en retard, sans effet runtime |
| `context/rules.json`, `context/memory.md` | prose org-authored citant l'ancien enum | prose en retard, sans effet runtime |
| `specs/*`, journaux, mémoires | histoire | conservent l'ancienne graphie à raison |

## Re-vérification

```bash
# 1. Le contrat : 2.0.0, la clé corrigée présente, la fautive absente :
python3 -c "import json; d=json.load(open('contracts/button.contract.json')); \
v=[p for p in d['props'] if p['name']=='variant'][0]; e=v['type']['enum']; \
print(d['version'], 'outlineNoir' in e, 'outilneNoir' not in e, \
v['bindings']['figma']['values']['outlineNoir'])"
# attendu : 2.0.0 True True Outline noir

# 2. Plus aucune surface vivante ne porte la faute (versionné seulement) :
git grep -l outilneNoir -- contracts src evals figma-sync examples packages parity extract
# attendu : AUCUNE sortie (exit 1) — toute sortie ici est une régression

# 3. L'instrument mesure à nouveau l'axe :
git grep -n "Style=Outline noir" -- extract/figma/visual-parity/subjects.ts
# attendu : 1 occurrence (le sujet re-pointé)
```

```js
// figma_execute, lecture seule — la source reste propre :
await figma.loadAllPagesAsync();
const set = await figma.getNodeByIdAsync('6:122');
const styles = set.variantGroupProperties['Style'].values;
console.log(styles, styles.includes('Outline noir'), styles.some(s => /utilne/i.test(s)));
// attendu : [...7 valeurs...] true false
```

Aucun re-relevé canvas n'a été fait pour CE reçu (fenêtre sans pont) : les faits canvas
cités sont ceux du `diagnosticVif` du 2026-08-05 et du journal O-16 ; les faits code
sont re-vérifiés au vif du 2026-08-06 sur le dépôt (commandes ci-dessus, exécutées).
