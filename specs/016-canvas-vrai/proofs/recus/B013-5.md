# Reçu B013-5 — `coordonnees` : les calques remis dans l'ordre visuel, à zéro pixel

**Date** : 2026-08-06 · **Lot** : `L-B013-5` (workflow multi-agents, agent canvas séquentiel §XI)
**Verdict : ✅ clos** · **Point de restauration** : version Figma nommée `016/B013-lots/avant`
**Journal** : `specs/016-canvas-vrai/decisions.md` O-16 · **Commit** : `0ed9981`

---

## Le défaut, re-relevé au vif (T026, 2026-08-05) — l'inversion confirmée, le mécanisme démenti

Méthode : `figma_execute` LECTURE SEULE — enfants du master `Coordonnees` (`2104:2904`)
lus par POSITION avec leurs coordonnées absolues (`registre/defauts-source.json`,
entrée B013-5, `diagnosticVif`).

| Position calque | Nom | x | Largeur | Côté visuel |
|---:|---|---:|---:|---|
| 0 | `wrapper` | 1192 | 576 | **DROITE** |
| 1 | `google-map` | 40 | 1152 | **GAUCHE** |

L'inversion visuelle est confirmée (le 1ᵉʳ calque d'auteur est à droite). Mais la
mémoire (2026-07-30) parlait d'un « **row-reverse d'auteur** » ; le vif a montré un
`layoutMode: GRID` — le placement ne suit pas l'ordre des enfants parce que c'est une
**grille à placement MANUAL**, pas parce qu'un axe est inversé. Le geste correctif a
donc été re-conçu au lot, comme le registre l'exigeait.

## Le geste (2026-08-06, nuit — journal O-16)

> « **B013-5** : coordonnees réordonné en ordre visuel, **écart 0 px** — le mécanisme
> réel était un GRID à placement MANUAL, pas un row-reverse (consigné) ; le contrat
> était déjà dans l'ordre normal, seule la prose d'inversion est retirée. »

Après geste, l'ordre des calques est `google-map` puis `wrapper` — l'ordre d'auteur
coïncide avec l'ordre visuel ET avec l'ordre des parts du contrat.

## Annonce ⟷ observé — le cycle pixel du lot

**Annonce** : rendu STRICTEMENT IDENTIQUE — l'inversion et le ré-ordonnancement se
compensent ; tout écart de pixel signale une erreur de geste et annule le lot.

**Observé** : **écart 0 px** au geste (O-16). Le cycle pixel global unique du batch
B013-1/5/7 + marqueurs (§XI, tenu par l'orchestrateur —
`.page-parity/FINAL18/verdict-vs-f17/verdict.md`) donne 6/9 maquettes byte-identiques ;
les 3 micro-diffs (a-propos, depannage-sav, contactez-nous 1 658 px dans une boîte
659×12) sont jugés au journal « anti-aliasing de re-rendu (contenu identique à
l'œil) », et vs référence pré-chantier : « rien de dégradé, a-propos AMÉLIORÉ
(189k→175k) ». L'attribution fine est celle du journal O-16, vérifiée en live par
l'orchestrateur.

## Contrepartie code — plus petite que planifiée, et c'est vérifié

Le plan du lot prévoyait « re-ordonner les parts pour suivre l'ordre réel des
calques ». Vérifié au vif du 2026-08-06 : les parts de
`contracts/coordonnees.contract.json` (v2.2.0) étaient **déjà** dans l'ordre visuel
(`['googleMap', 'wrapper']`) — c'est le canvas qui a rejoint le contrat, pas
l'inverse. Seule la **prose d'inversion** de la description de `googleMap` est retirée
(diff `0ed9981` : « l'ordre d'auteur du calque est un artefact sans portée » →
« B013-5 clos […] l'ordre des parts suit l'ordre RÉEL des calques, qui coïncide
désormais avec l'ordre visuel »). **Aucun bump** : seule une `description` change
(aucune prop, aucun binding, aucun canal).

## La ligne `instrument=1` de `measure:gate` — re-jugée, destination nommée

Le registre demandait de « re-juger la ligne instrument=1 » au compte vif. Le gate
post-lot (archivé : `proofs/measure-gate-post-dw.txt`, verdict PASS) imprime encore
`instrument=1` : cette ligne lit le **registre 014**
(`specs/014-mesure-juste-triage/proofs/registre/causes.json`, organismLine
`coordonnees/coordonnees-master-defaults`, rawPct 0,52 %, re-classe 015
`org-coordonnees-geometry-reclasse`) dont **l'unique fait divergent**
(`coordonnees.structure.colonnes-ordre` : part order ≠ « row-reverse » d'auteur) est
exactement l'inversion que CE lot vient de résorber à la source. Un re-relevé du fait
contre un dump frais ne montrerait plus d'inversion. Le `resolvedBy` additif sur cette
ligne du registre 014 **reste à poser** — nommé ici, pas fait en douce (hors périmètre
de ce reçu ; T034 n'a traité que DW-002/DW-003).

## Re-vérification

```js
// figma_execute, lecture seule — ordre des calques = ordre visuel :
await figma.loadAllPagesAsync();
const m = await figma.getNodeByIdAsync('2104:2904');
console.log(m.children.map(c => ({ nom: c.name, x: Math.round(c.absoluteBoundingBox.x) })));
// attendu : [{nom:'google-map', x≈40}, {nom:'wrapper', x≈1192}] — google-map EN PREMIER
```

```bash
# côté contrat — ordre des parts + prose corrigée, version inchangée :
python3 -c "import json; d=json.load(open('contracts/coordonnees.contract.json')); \
r=d['anatomy']['root']; print(d['version'], list(r['parts'].keys()), \
'B013-5 clos' in r['parts']['googleMap']['description'])"
# attendu : 2.2.0 ['googleMap', 'wrapper'] True
```

Aucun re-relevé canvas n'a été fait pour CE reçu (fenêtre sans pont) : les faits canvas
cités sont ceux du journal O-16 et du `diagnosticVif` du 2026-08-05, sourcés ligne par
ligne. **Restauration** si besoin : historique de versions Figma → `016/B013-lots/avant`.
