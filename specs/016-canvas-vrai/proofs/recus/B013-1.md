# Reçu B013-1 — les 4 propriétés orphelines supprimées de leurs masters

**Date** : 2026-08-06 · **Lot** : `L-B013-1` (workflow multi-agents, agent canvas séquentiel §XI)
**Verdict : ✅ clos** · **Point de restauration** : version Figma nommée `016/B013-lots/avant`
**Journal** : `specs/016-canvas-vrai/decisions.md` O-16 · **Commit** : `0ed9981`

---

## Le défaut, re-relevé au vif (T026, 2026-08-05) — la mémoire confirmée à l'identique

Méthode du relevé : `figma_execute` LECTURE SEULE — `componentPropertyDefinitions` de
chaque master confronté aux `componentPropertyReferences` de TOUT son sous-arbre
(`registre/defauts-source.json`, entrée B013-1, `diagnosticVif`).

| Master | nodeId | Définitions | Orphelines (référencées par rien) | Reste après suppression |
|---|---|---|---|---:|
| Presentation | `2103:2824` | `Titre#2103:53`, `Texte#2103:54`, `Bouton#2103:55` | `Titre#2103:53` | 2 |
| SAV | `2108:3105` | `Titre#2108:60` | `Titre#2108:60` (sa SEULE propriété) | **0** |
| Coordonnees | `2104:2904` | `Accroche#2104:57`, `Titre#2104:58` | les DEUX | **0** |

Écart avec le diagnostic d'origine (mémoire `figma-cleanup-backlog-013`, 2026-07-30) :
**aucun** — les 4 étaient encore définies et encore orphelines au vif.

## Le geste (2026-08-06, nuit — journal O-16)

> « **B013-1** : 4 propriétés orphelines supprimées (presentation Titre#2103:53, sav
> Titre#2108:60, coordonnees Accroche#2104:57 + Titre#2104:58) — **orphelinage re-prouvé
> atomiquement dans le script de suppression**. Version nommée 016/B013-lots/avant. »

Le script de suppression a re-vérifié l'orphelinage **dans le même geste** (garde contre
un relevé périmé sur fichier vivant — même règle que L-DW002) : une propriété devenue
référencée entre le relevé et le geste aurait fait refuser sa suppression.

**Incident traité dans la même fenêtre** (O-16) : SAV (`2108:3105`) et Coordonnees
(`2104:2904`) ne portaient AUCUN marqueur `ds_contracts` — posés par l'orchestrateur
(l'identité par marqueur vaut pour 25 autres masters, ces deux-là avaient échappé aux
vagues). Un marqueur est du plugin-data : il ne peint rien.

## L'annonce ⟷ l'observé — le cycle pixel global du lot (§XI)

**Annonce** : ZÉRO écart visuel — une propriété que rien ne référence ne peint rien ;
tout pixel qui bouge annule le lot.

**Observé** (cycle pixel global unique autour de tout le batch B013-1/5/7 + marqueurs,
tenu par l'orchestrateur — `.page-parity/FINAL18/verdict-vs-f17/verdict.md`, exit 1) :

| Constat | Détail |
|---|---|
| **6/9 maquettes byte-identiques** | accueil, motorisation, portes-de-garage, portes-entrée, industrielles, résidentielles |
| 3 micro-diffs | a-propos 31 030 px, depannage-sav 11 776 px, contactez-nous 1 658 px (boîte 659×12 : une ligne de texte) |
| Jugement (O-16) | « micro-diffs d'**anti-aliasing de re-rendu** (contenu identique à l'œil) » ; vs référence pré-chantier : « rien de dégradé, a-propos AMÉLIORÉ (189k→175k) » |

Les trois maquettes en micro-diff portent toutes des sections re-rendues par la fenêtre ;
les trois masters touchés par CE lot (presentation, sav, coordonnees) apparaissent aussi
sur des pages restées **byte-identiques à zéro pixel** — cohérent avec « une propriété
orpheline ne peint rien ». L'attribution fine est celle du journal O-16, vérifiée en
live par l'orchestrateur.

## Contrepartie code : aucune — et c'est vérifié, pas supposé

`promotionCodeSide: null` (pas de bump) : aucun contrat ne portait ces propriétés.
Vérifié au vif du 2026-08-06 : `contracts/sav.contract.json` v1.2.0 lie `titre` à la
propriété TEXT « Titre » **du SectionHeader composé** (forwarding `titre: "{titre}"`) —
la définition supprimée `Titre#2108:60` était une coquille vide locale au master,
référencée par aucun nœud, donc jamais le support de ce binding.

**Conséquence nommée, pas tue** : SAV et Coordonnees se retrouvent **sans aucune
propriété propre** — exactement ce que le `diagnosticVif` annonçait
(`resteApresSuppression: 0`). Exposer des propriétés Texte HONNÊTES (définies ET
référencées) sur `hero` et `sav` est le lot **B013-4 (T041/T042), toujours ouvert** —
la suppression des coquilles vides n'y supplée pas.

## Re-vérification

```js
// figma_execute, lecture seule — à exécuter hors fenêtre d'amend (protocole O-9)
await figma.loadAllPagesAsync();
const attendu = {
  '2103:2824': ['Texte#2103:54', 'Bouton#2103:55'],   // Presentation
  '2108:3105': [],                                     // SAV
  '2104:2904': [],                                     // Coordonnees
};
const disparues = ['Titre#2103:53', 'Titre#2108:60', 'Accroche#2104:57', 'Titre#2104:58'];
for (const [id, defsAttendues] of Object.entries(attendu)) {
  const m = await figma.getNodeByIdAsync(id);
  const defs = Object.keys(m.componentPropertyDefinitions ?? {});
  console.log(m.name, defs,
    'OK=', defs.sort().join()===defsAttendues.sort().join()
        && !defs.some(d => disparues.includes(d)));
}
// attendu : Presentation → 2 props restantes ; SAV → [] ; Coordonnees → [] ; aucune des 4 ne réapparaît
```

Aucun re-relevé canvas n'a été fait pour CE reçu (fenêtre sans pont) : les faits canvas
cités sont ceux du journal O-16 et du `diagnosticVif` du 2026-08-05, sourcés ligne par
ligne. **Restauration** si besoin : historique de versions Figma → `016/B013-lots/avant`.
