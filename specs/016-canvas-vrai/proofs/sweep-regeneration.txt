# Sweep — régénération avec les 4 correctifs · 2026-08-05T19:06:39Z
### $ npm run build

> ds-contracts-poc@0.7.0 build
> npm run tokens && npm run schema && npm run generate


> ds-contracts-poc@0.7.0 tokens
> node scripts/build-tokens.mjs

✔ Tokens built: 223 custom properties (:root), 0 dark-mode overrides, 1 brand mode(s): default

> ds-contracts-poc@0.7.0 schema
> tsx scripts/emit-schema.ts

✔ contracts/contract.schema.json emitted from Zod schema

> ds-contracts-poc@0.7.0 generate
> tsx scripts/generate-components.ts

✔ Generated 34 component(s) from contracts: AccordionRow, Avantage, Button, CarouselControls, Carte, Checkbox, Coordonnees, Copyright, Devis, Equipe, FAQ, Field, Footer, FooterColumn, Formulaire, GoogleReviews, Header, Hero, Input, MemberCard, MemberPicture, NavItem, PiquerayLogo, Presentation, ProductCard, Realisation, Reassurances, ReviewCard, SAV, SectionHeader, Select, Tab, Textarea, TexteSEO
### => OK

### $ npm run parity

> ds-contracts-poc@0.7.0 parity
> tsx parity/diff.ts

⚠ Figma naming bridged (the file does not use this repo's collection/mode names):
    · mode "Dark" does not exist in collection "Semantic" — read from its only mode, "Light"

✖ 21 drift finding(s) (+3 acknowledged in parity/baseline.json):
    figma — behind: 19
    figma-tokens — mismatch: 2

  [figma BEHIND] FAQ.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Footer.PiquerayLogo
    Contract composes ds.piqueray-logo but no PiquerayLogo instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Footer.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Footer.FooterColumn
    Contract composes ds.footer-column but no FooterColumn instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Footer.Copyright
    Contract composes ds.copyright but no Copyright instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.SectionHeader
    Contract composes ds.section-header but no SectionHeader instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Avantage
    Contract composes ds.avantage but no Avantage instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Field
    Contract composes ds.field but no Field instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Field
    Contract composes ds.field but no Field instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Field
    Contract composes ds.field but no Field instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Field
    Contract composes ds.field but no Field instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Field
    Contract composes ds.field but no Field instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Field
    Contract composes ds.field but no Field instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Field
    Contract composes ds.field but no Field instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Formulaire.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Header.NavItem
    Contract composes ds.nav-item but no NavItem instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] Header.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [figma-tokens MISMATCH] Primitives/size/accordion-row/trigger [Value]
    tokens/ says 32, Figma says 64
    proposed patch: {"tokenPath":"size.accordion-row.trigger","mode":"Value","adoptFigmaValue":64}
    → Adopt into tokens/ (promotion) then npm run tokens — or push tokens/ to Figma via figma_import_tokens

  [figma-tokens MISMATCH] Primitives/size/accordion-row/trigger-petit [Value]
    tokens/ says 24, Figma says 40
    proposed patch: {"tokenPath":"size.accordion-row.trigger-petit","mode":"Value","adoptFigmaValue":40}
    → Adopt into tokens/ (promotion) then npm run tokens — or push tokens/ to Figma via figma_import_tokens

  — acknowledged (baselined, does not fail the check) —

  [figma BEHIND] Carte.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [figma BEHIND] SectionHeader.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [icons AHEAD] assets/icons/close.svg
    code has an icon asset with no registry entry (Figma-first: every icon is born in Figma, FR-008)
    → Review: add to the registry (promotion, requires a Figma master) or delete the orphaned asset

### => ECHEC (exit 1)

### $ npm run plugin:check

> ds-contracts-poc@0.7.0 plugin:check
> node scripts/plugin-engine-check.mjs

file:///Users/dlstudio/.superset/projects/ds-contract/scripts/build-plugin-zip.mjs:248
    throw new Error(
          ^

Error: plugin-zip: the plugin engine bundle is STALE vs core — a fresh bundle hashes 82dec4a2c2cd… (574598 bytes) but the committed receipt records c82fda5bbd2f… (572344 bytes). Core (or tokens/contracts/icons) changed without re-recording. Review the change, then run `node scripts/build-plugin-zip.mjs --update-engine-receipt` and commit engine.receipt.json.
    at verifyEngineReceipt (file:///Users/dlstudio/.superset/projects/ds-contract/scripts/build-plugin-zip.mjs:248:11)
    at async file:///Users/dlstudio/.superset/projects/ds-contract/scripts/plugin-engine-check.mjs:62:1

Node.js v24.14.0
### => ECHEC (exit 1)

### $ npx tsx scripts/deterministic-roundtrip.mjs

DETERMINISTIC ROUND-TRIP — ds.google-reviews@1.0.0 (GoogleReviews)

1. contract → canvas  (the plugin engine, run twice)
  ✔ built GoogleReviews both times; node trees byte-identical (1447 bytes fingerprint) — DETERMINISTIC
  ✔ the repeat+component produced 5 nested instances in "groupeCartes" — dependency depth exercised

2. canvas → contract  (dump attempt; core proof: step 1 byte-identical ×2)
  ✔ proposeDiff recovered a contract — element: <div>, props: 5
  ✔ round-trip closes: the anatomy that went to canvas came back (byte-identical ×2 proves the loop)

3. contract → code  (emit React from the contract)
  ✔ emitted 7585B of React from the same contract — <section> host, repeat+component, text props typed

✔ THE FULL LOOP RAN WITH ZERO AI — pure deterministic functions (the same engine the plugin runs):
    contract → canvas → contract → code, byte-reproducible.
  The AI built this tooling; it is NEVER in the conversion. That is the guarantee.
  ✅ Depth restored (spec 006): repeat+component (5 nested instances) is now exercised.

### => OK

### $ node scripts/core-browser-check.mjs
✔ core barrel bundles for platform=browser: 11.89 MB raw, 5.21 MB minified
✔ all 4 emitters ran in a VM with no node globals (AccordionRow): react=3469+4178+2301B, html=3577+4676B, react-inline=7051B, figma-script=57777B
### => OK

### $ npx tsc --noEmit
### => OK

### $ npx tsc -p tsconfig.build.json
### => OK

### $ npm run geometry:gate

> ds-contracts-poc@0.7.0 geometry:gate
> tsx extract/geometry-gate/run.ts

geometry:gate — verdict: PASS (exit 0)
Comptage (en direct, jamais figé) :
  contracts 34 · geometric entries 2 · governed refs 219 · named literals 2 · invisible 0
✓ zero invisible literal, zero registry refusal
### => OK

