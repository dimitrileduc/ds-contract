=== T005 — npm run parity (AVANT adoption) — 2026-07-29T15:12:27Z ===

> ds-contracts-poc@0.7.0 parity
> tsx parity/diff.ts

⚠ Figma naming bridged (the file does not use this repo's collection/mode names):
    · mode "Dark" does not exist in collection "Semantic" — read from its only mode, "Light"

✖ 78 drift finding(s) (+4 acknowledged in parity/baseline.json):
    figma-tokens — mismatch: 1, ahead: 77

  [figma-tokens MISMATCH] Primitives/font/family/montserrat [Value]
    tokens/ says Montserrat, sans-serif, Figma says Montserrat
    proposed patch: {"tokenPath":"font.family.montserrat","mode":"Value","adoptFigmaValue":"Montserrat"}
    → Adopt into tokens/ (promotion) then npm run tokens — or push tokens/ to Figma via figma_import_tokens

  [figma-tokens AHEAD] Primitives/color/noir-pur
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/color/gris-clair
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/size/44
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/size/54
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/weight/bold
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/16
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/20
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/24
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/25
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/27
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/30
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/40
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/48
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/50
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/60
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/68
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/letter-spacing/15
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/8
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/12
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/24
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/48
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/64
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/89
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/96
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/128
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/392
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/radius/500
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/font/line-height/32
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Primitives/space/597
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-1/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-2/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-3/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-4/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-5/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-6/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/paragraphe/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/lead/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-hero/family
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-hero/size
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-hero/weight
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/titre-hero/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/libelle-bouton/family
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/libelle-bouton/size
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/libelle-bouton/weight
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/libelle-bouton/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/paragraphe-gras/family
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/paragraphe-gras/size
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/paragraphe-gras/weight
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  [figma-tokens AHEAD] Semantic/typography/paragraphe-gras/line-height
    Figma variable has no counterpart in tokens/
    → Review + add to tokens/ (promotion) or delete the variable

  …and 28 more (see parity/report.json)

  — acknowledged (baselined, does not fail the check) —

  [figma BEHIND] Carte.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [figma MISMATCH] Presentation.Texte (default)
    Default differs — contract: "Depuis plus de 50 ans, la société Piqueray est une référence en Province de Liège. Aujourd’hui dirigée par Florian et Cécilia Piqueray, l’entreprise perpétue les valeurs de proximité et d’excellence technique. Dépositaire officiel Hörmann, nous allions la force d’un leader mondial à la souplesse d’une PME locale.", figma: "Depuis plus de 50 ans, la société Piqueray est une référence en Province de Liège. Aujourd'hui dirigée par Florian et Cécilia Piqueray, l'entreprise perpétue les valeurs de proximité et d'excellence technique. Dépositaire officiel Hörmann, nous allions la force d'un leader mondial à la souplesse d'une PME locale."
    → Adopt into contract (promotion) or reset the property default

  [figma BEHIND] SectionHeader.Bouton
    Contract composes ds.button but no Bouton instance exists inside the Figma component
    → Re-run the component sync script

  [icons AHEAD] assets/icons/close.svg
    code has an icon asset with no registry entry (Figma-first: every icon is born in Figma, FR-008)
    → Review: add to the registry (promotion, requires a Figma master) or delete the orphaned asset

EXIT_CODE=1
