# US3 governance receipt

`npm run eval` passed **221/221** deterministic cases, including
`odoo-figma-links-governance`. The case runs the real Figma-links generator in
a scratch repository and proves the following named outcomes:

- all 13 root/shell and six child panels, including HeroVideo, project from their canonical contract;
- missing and duplicate mappings fail the reviewed census;
- absent contracts, divergent versions, unreachable paths, null anchors and
  malformed file keys or node IDs are emitted as named `unavailable` states and
  make `--check` fail;
- an input-anchor mutation changes the generated node identity; and
- full Figma URLs and native/third-party selectors are refused.

The static module gate also passed with the generated table before its consumer
asset and exactly one generic action, option and control.
